DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=
DB_DATABASE=feedme

# SMTP — forgot-password OTP email (Gmail example)
# Leave SMTP_USER empty to print OTP in backend terminal (dev mode)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

# Optional
# JWT_SECRET=feedme-dev-secret
# JWT_EXPIRES_IN=7d
# PORT=3000
